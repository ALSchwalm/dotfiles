;;; extensions.el --- alschwalm Layer extensions File for Spacemacs
;;
;; This file is not part of GNU Emacs.
;;
;;; License: GPLv3

(setq alschwalm-pre-extensions
      '(
        ;; pre extension names go here
        ))

(setq alschwalm-post-extensions
      '(
        ;; post extension names go here
        ))

;; For each extension, define a function alschwalm/init-<extension-name>
;;
;; (defun alschwalm/init-my-extension ()
;;   "Initialize my extension"
;;   )
;;
;; Often the body of an initialize function uses `use-package'
;; For more info on `use-package', see readme:
;; https://github.com/jwiegley/use-package
